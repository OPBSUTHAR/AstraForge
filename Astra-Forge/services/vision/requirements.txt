fastapi==0.115.6
uvicorn[standard]>=0.34.0
pillow>=11.0.0
numpy>=2.1.0
# AstraForge vision is intentionally fully offline:
# only Pillow + numpy are required to convert a 2D image into a mesh.
# Optional backends (installed separately, very heavy):
#   pip install rembg onnxruntime
#   pip install trellis
#   pip install sf3d
